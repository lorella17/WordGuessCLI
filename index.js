var Word = require("./Word");
var inquirer = require ("inquirer");

var wordBank = [ "United", "Braves", "Falcons", "Georgia",
    "Patriots", "Trump", "Javascript", "mysql"
];

const guesses;
const word;
const selectedWord;
const wordSelected;
